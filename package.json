{
  "name": "mfg-bot-backend",
  "version": "1.0.0",
  "description": "MFG_bot WhatsApp automation backend for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm run build",
    "build:start": "cd client && npm run build && node server.js"
  },
  "dependencies": {
    "baileys": "^6.7.16",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pino": "^8.21.0",
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": "18.x"
  }
}
